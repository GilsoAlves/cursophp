<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Outras Funções aritimeticas</title>
</head>
<body>
    <?php 
    // $r = abs(-2000); #função abs retira o sinal
    // print("A resposta é $r");
    
   # $r = base_convert(254, 10, 2); # 254 é o meu numero 10 é a minha base e 8 é a base que será convertida de 10 para 8
   # print("A resposta é $r");

   //ceil() arredonda pra cima , floor() arrendonda para baixo, round() arrendonda
    //hypot() tamanho do cateto

    
    #$r = 5/2; # intdiv()
    #print("A resposta é $r");

      // $r = max(50,24,14,80); // mim(), max()
      // print("A resposta é $r");

       # $r = pi();  #M_PI é uma constante que ja vem declarada no php.
       # print("A resposta é $r");

      // $r = 5**2; // potencia de 5
      // print("A resposta é $r");

       # $r = pow(5 , 2); #potencia de 5 usando esse metodo vc perde a ordem de procedencia.
       # print("A resposta é $r");

      # $r = sqrt(81); # calcula a raiz quadrada
      # print("A resposta é $r");

       //$r = sqrt(81); # calcula a raiz quadrada
      // print("A resposta é $r");

       //$r = sqrt(81); # calcula a raiz quadrada
      // print("A resposta é $r");

      $r= "5" + "5";
      var_dump($r) ;


    ?>
    
</body>
</html>
